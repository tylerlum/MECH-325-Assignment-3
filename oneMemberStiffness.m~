function k = oneMemberStiffness(E,D,d,t)

%ENQ 8-20 assuming that diameter of bolt head is 1.5 times bolt diameter
%book says this is reasonable
numerator = 0.5774*pi*E*d;
logNum = (1.155 * t + 0.5*d) * (2.5*d);
logDen = (1.155 * t + 2.5*d) * (0.5*d);

k = numerator/log(logNum/logDen);

